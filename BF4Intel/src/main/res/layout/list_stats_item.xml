<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  style="@style/RegularCard"
  >

  <RelativeLayout
    android:id="@+id/wrap_top"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >

    <TextView
      android:id="@+id/index"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:textAppearance="@style/TextAppearance.Holo.XLarge"
      />

    <include
      layout="@layout/service_stars_count"
      android:id="@+id/wrap_servicestar"
      android:layout_width="@dimen/service_star_count_width"
      android:layout_height="wrap_content"
      android:layout_alignParentRight="true"
      android:layout_centerVertical="true"
      />

    <LinearLayout
      android:id="@+id/wrap_image"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_toLeftOf="@id/wrap_servicestar"
      android:layout_toRightOf="@id/index"
      android:gravity="center"
      >

      <ImageView
        android:id="@+id/item_image"
        android:layout_width="@dimen/stats_item_width"
        android:layout_height="wrap_content"
        android:contentDescription="@string/cdesc_item_image"
        android:adjustViewBounds="true"
        android:layout_marginTop="@dimen/spacing_m"
        android:layout_marginBottom="@dimen/spacing_m"
        android:src="@drawable/w_a91_fancy"
        />
    </LinearLayout>
  </RelativeLayout>

  <TextView
    android:id="@+id/item_name"
    style="@style/TextViewLightGrey"
    android:layout_below="@id/wrap_top"
    />

  <TextView
    android:id="@+id/kill_count"
    style="@style/TextViewLightGrey"
    android:layout_alignParentRight="true"
    android:layout_below="@id/wrap_top"
    />

  <ProgressBar
    style="?android:attr/progressBarStyleHorizontal"
    android:id="@+id/item_progress"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:max="100"
    android:layout_below="@id/item_name"
    />
</RelativeLayout>
